name: static analysis

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - develop
      - main
      - release/**
      - feature/**
      - hotfix/**
  push:
    branches:
      - develop
      - main
      - release/**
      - feature/**
      - hotfix/**

jobs:
  call-workflow-jira-issue:
    uses: MiraGeoscience/DevTool/.github/workflows/python/static-analysis.yml@devops-427
